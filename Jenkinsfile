pipeline{
    agent any
    tools { 
        maven 'Maven' 
        jdk 'java8' 
    }
    stages{
        stage('Build'){
            steps{
               echo 'done'
               echo "PATH = ${PATH}"
               echo "M2_HOME = ${M2_HOME}"
            }
        }
    }
}
