pipeline{
    agent any
    stages{
        stage('Build'){
            steps{
               echo 'done'
               echo "PATH = ${PATH}"
               echo "M2_HOME = ${M2_HOME}"
            }
        }
    }
}
